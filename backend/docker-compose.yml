services:
  node-app:
    build:
      context: .
      args:
        NODE_ENV: production
    ports:
      - "3000:3000"
    env_file:
      - ./.env
    command: node src/index.js

  redis:
    image: redis